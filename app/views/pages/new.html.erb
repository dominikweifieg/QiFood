<% title t('pages.new.title')%>

<% form_for(@page) do |f| %>
  <%= f.error_messages %>
	<%= render :partial => 'editor' , :locals => {:f => f} %>
  <p>
    <%= f.submit "Create" %>
  </p>
<% end %>

<%= link_to 'Back', pages_path %>
