<%= link_to_function('&or;', nil, :class => "pagination") do |page|
	page[:sort_by].value = value
	page[:sort_dir].value = 'ASC'
	page[:finder].submit
end %><%= link_to_function('&and;', nil, :class => "pagination") do |page|
	page[:sort_by].value = value
	page[:sort_dir].value = 'DESC'
	page[:finder].submit
end %>



